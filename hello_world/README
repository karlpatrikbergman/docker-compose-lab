This an example published on Digital Ocean:
ihttps://www.digitalocean.com/community/tutorials/how-to-configure-a-continuous-integration-testing-environment-with-docker-and-docker-compose-on-ubuntu-14-04

It is a simple web app that counts visits and ouputs number of visits
as html.

Redis:
In-memory data structure server
NoSQL database

Run application:
docker-compose -f ~/hello_world/docker-compose.yml build
docker-compose -f ~/hello_world/docker-compose.yml up -d

Inspect docker container "web" and open ip in browser.

The example also contains an example of how to use docker-compose for setting
up automatic functional acceptance tests.
